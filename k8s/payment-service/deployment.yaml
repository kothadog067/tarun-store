apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-service
  namespace: tarun-store
  labels:
    app: payment-service
    tags.datadoghq.com/service: "payment-service"
    tags.datadoghq.com/env: "production"
    tags.datadoghq.com/version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: payment-service
  template:
    metadata:
      labels:
        app: payment-service
        tags.datadoghq.com/service: "payment-service"
        tags.datadoghq.com/env: "production"
        tags.datadoghq.com/version: "1.0.0"
      annotations:
    spec:
      containers:
        - name: payment-service
          image: gcr.io/PROJECT_ID/payment-service:IMAGE_TAG
          ports:
            - containerPort: 8005
          env:
            - name: DD_SERVICE
              value: "payment-service"
            - name: DD_ENV
              value: "production"
            - name: DD_VERSION
              value: "1.0.0"
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DD_TRACE_AGENT_PORT
              value: "8126"
            - name: DD_LOGS_INJECTION
              value: "true"
            - name: DD_RUNTIME_METRICS_ENABLED
              value: "true"
            - name: DD_PROFILING_ENABLED
              value: "true" 
          readinessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8005
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              memory: 128Mi
              cpu: 25m
            limits:
              memory: 256Mi
              cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: tarun-store
  labels:
    app: payment-service
spec:
  selector:
    app: payment-service
  ports:
    - port: 8005
      targetPort: 8005
      name: http
