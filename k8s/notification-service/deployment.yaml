apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: tarun-store
  labels:
    app: notification-service
    tags.datadoghq.com/service: "notification-service"
    tags.datadoghq.com/env: "production"
    tags.datadoghq.com/version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        tags.datadoghq.com/service: "notification-service"
        tags.datadoghq.com/env: "production"
        tags.datadoghq.com/version: "1.0.0"
      annotations:
        admission.datadoghq.com/python-lib.version: "latest"
    spec:
      containers:
        - name: notification-service
          image: gcr.io/PROJECT_ID/notification-service:IMAGE_TAG
          ports:
            - containerPort: 8006
          env:
            - name: DD_SERVICE
              value: "notification-service"
            - name: DD_ENV
              value: "production"
            - name: DD_VERSION
              value: "1.0.0"
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DD_TRACE_AGENT_PORT
              value: "8126"
            - name: DD_LOGS_INJECTION
              value: "true"
            - name: DD_RUNTIME_METRICS_ENABLED
              value: "true"
            - name: DD_PROFILING_ENABLED
              value: "true" 
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              memory: 128Mi
              cpu: 100m
            limits:
              memory: 256Mi
              cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: tarun-store
  labels:
    app: notification-service
spec:
  selector:
    app: notification-service
  ports:
    - port: 8006
      targetPort: 8006
      name: http
