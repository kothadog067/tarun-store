apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: tarun-store
  labels:
    app: product-service
    tags.datadoghq.com/service: "product-service"
    tags.datadoghq.com/env: "production"
    tags.datadoghq.com/version: "1.0.0"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
        tags.datadoghq.com/service: "product-service"
        tags.datadoghq.com/env: "production"
        tags.datadoghq.com/version: "1.0.0"
      annotations:
        admission.datadoghq.com/python-lib.version: "latest"
    spec:
      containers:
        - name: product-service
          image: gcr.io/PROJECT_ID/product-service:IMAGE_TAG
          ports:
            - containerPort: 8001
          env:
            - name: DD_SERVICE
              value: "product-service"
            - name: DD_ENV
              value: "production"
            - name: DD_VERSION
              value: "1.0.0"
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DD_TRACE_AGENT_PORT
              value: "8126"
            - name: DD_LOGS_INJECTION
              value: "true"
            - name: DD_RUNTIME_METRICS_ENABLED
              value: "true"
            - name: DD_PROFILING_ENABLED
              value: "true" 
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tarun-store-secrets
                  key: postgres-password
            - name: DATABASE_URL
              value: "postgresql://tarun:$(POSTGRES_PASSWORD)@postgres:5432/products_db"
          readinessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8001
            initialDelaySeconds: 20
            periodSeconds: 20
          resources:
            requests:
              memory: 128Mi
              cpu: 25m
            limits:
              memory: 256Mi
              cpu: 500m
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: tarun-store
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  ports:
    - port: 8001
      targetPort: 8001
      name: http
